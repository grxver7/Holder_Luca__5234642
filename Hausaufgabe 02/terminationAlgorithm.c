#include "terminationAlgorithm.h"
double terminationAlgorithm (double a, double b) {
    for (int i =1; i <=1000; i++) {
        if (b == a) {

        }
        if (a-b <= 0.00001) {

        }
    }
}